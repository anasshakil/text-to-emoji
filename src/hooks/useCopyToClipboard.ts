'use client';

// import { useCallback } from "react";

// export const handleCopy = useCallback(async () => {
//     try {
//       await navigator.clipboard.writeText(emojis);
//       setIsCopied(true);
//     } catch (err) {
//       console.error('Failed to copy text:', err);
//     }
//   }, [emojis]);
